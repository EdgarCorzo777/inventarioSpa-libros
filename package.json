{
  "name": "librostock",
  "version": "1.0.0",
  "description": "SPA de gestión de inventario para librería",
  "scripts": {
    "server": "json-server --watch db.json --port 3001",
    "dev": "npx live-server src --port=3000",
    "start": "npm run server"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  }
}
